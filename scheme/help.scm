; Scheme library for help
; with MyScheme. Simple.

(write "MYSCHEME"                                         )
(write "    MyScheme is a Scheme interpreter I hacked"    )
(write "    together in an afternoon. It's... Functional.")
(write "USAGE"                                            )
(write "    myscm <args>"                                 )
(write "FLAGS"                                            )
(write "    -d: Enable debug mode."                       )
(write "    -v: Print version and exit."                  )
(write "TODO"                                             )
(write "    - Add macro support."                         )
(write "    - Add user input support."                    )
(write "    - Add GUI support."                           )
